spring:
  datasource:
    url: jdbc:mysql://localhost:3306/articles
    username: root
    password: test16

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
server:
  port: 9000

